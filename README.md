# 🚀 Python Excel 自動化彙整工具 | Automated Report Aggregator

### 📖 專案簡介 (Introduction)
在金融業後台與行政作業中，經常需要處理來自不同分行或部門寄來的 Excel 報表。傳統的人工整併方式不僅耗時（打開、複製、貼上），且容易發生人為錯誤。

本專案開發了一個 **Python 自動化腳本**，能夠**一鍵掃描**指定資料夾內的所有 Excel 檔案，將其合併為一張總表，並自動計算業績小計，協助團隊將重複性作業的時間成本降至最低。

---

### 💡 解決痛點 (Problem Solved)
* **❌ Before (人工處理)**：
    * 每天收到 10+ 份格式相同的 Excel 檔。
    * 手動打開每個檔案 -> 複製資料 -> 貼到總表 -> 拉 Excel 公式計算總和。
    * **耗時**：約 60 分鐘/天。
    * **風險**：容易貼錯行、公式拉錯。

* **✅ After (Python 自動化)**：
    * 執行腳本 -> 系統自動掃描 -> 合併 -> 產出報表。
    * **耗時**：**< 5 分鐘**。
    * **風險**：0% (邏輯固定，數據準確)。

---

### 🛠️ 功能特色 (Key Features)
1.  **批次讀取 (Batch Processing)**：使用 `glob` 自動抓取資料夾內所有 `.xlsx` 檔案，無需手動指定檔名。
2.  **智能合併 (Smart Merge)**：利用 `Pandas` 將多份表格垂直堆疊 (Concat)，自動對齊欄位。
3.  **自動統計 (Auto Summary)**：腳本內建數據分析功能，自動產出「分行/產品別」的業績小計 (Pivot Table)。
4.  **多分頁輸出 (Multi-sheet Export)**：最終報表包含 `總明細 (Master Data)` 與 `統計表 (Summary)` 兩個分頁，方便主管查閱。

---

### 💻 技術堆疊 (Tech Stack)
* **Language**: Python 3
* **Core Library**: `Pandas` (數據處理核心)
* **Modules**: `glob`, `os` (檔案系統操作)
* **Export Engine**: `openpyxl` (Excel 輸出引擎)

---

### 📊 模擬情境 (Simulation)
本專案包含一段模擬程式碼，會自動在 `Branch_Reports` 資料夾中生成三份模擬的分行報表（台北、台中、高雄），並執行合併作業，產出 `Final_Master_Report.xlsx`。

---
*Created by Hsin Kang*
